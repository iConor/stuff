#pragma once


namespace render
{
    struct layout_desc
    {
        const char* semantic;
        int size;
    };


    class layout
    {
    public:
        // layout(const layout_desc* desc, int count);
        // ~layout();
    };
}
