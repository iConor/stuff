#pragma once


namespace renderer
{
    struct layout_desc
    {
        const char* semantic;
        int size;
    };

    class layout
    {
    public:
        // layout();
        // ~layout();
    };
}
