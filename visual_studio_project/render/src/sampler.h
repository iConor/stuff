#pragma once


namespace render
{
    struct sampler_desc
    {
        void* data;
    };


    class sampler
    {
    public:
        // sampler(const sampler_desc& desc);
        // ~sampler();
    };
}
